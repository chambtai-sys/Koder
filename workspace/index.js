// Welcome to Koder IDE!
// This is your JavaScript workspace.
// Press 'Run Code' or Ctrl+Enter to execute this file.

console.log("Hello from Koder JS Workspace!");

const greet = (name) => {
    return `Hello, ${name}! Welcome to Koder, the JS & C# Web IDE.`;
};

console.log(greet("Developer"));

// You can perform math operations, loops, etc.
for (let i = 1; i <= 3; i++) {
    console.log(`Running iteration ${i}...`);
}
